"use strict";
/**
 * Форматирование Markdown и HTML для сообщений MAX. Функции не экранируют
 * текст автоматически: пользовательские данные нужно передать в escape/escapeHtml.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.escapeHtml = exports.linkHtml = exports.preHtml = exports.codeHtml = exports.strikethroughHtml = exports.underlineHtml = exports.italicHtml = exports.boldHtml = exports.escape = exports.link = exports.pre = exports.code = exports.strikethrough = exports.italic = exports.bold = void 0;
const bold = (text) => `**${text}**`;
exports.bold = bold;
const italic = (text) => `_${text}_`;
exports.italic = italic;
const strikethrough = (text) => `~~${text}~~`;
exports.strikethrough = strikethrough;
const code = (text) => `\`${text}\``;
exports.code = code;
const pre = (text, language = '') => {
    return language
        ? `\`\`\`${language}\n${text}\n\`\`\``
        : `\`\`\`\n${text}\n\`\`\``;
};
exports.pre = pre;
const link = (text, url) => `[${text}](${url})`;
exports.link = link;
/** Экранирует пользовательский текст перед вставкой в Markdown. */
const escape = (text) => {
    // Экранируем все символы разметки независимо от места вставки текста.
    return text.replace(/([_*[\]()~`>#+=|{}.!\\-])/g, '\\$1');
};
exports.escape = escape;
const boldHtml = (text) => `<b>${text}</b>`;
exports.boldHtml = boldHtml;
const italicHtml = (text) => `<i>${text}</i>`;
exports.italicHtml = italicHtml;
const underlineHtml = (text) => `<u>${text}</u>`;
exports.underlineHtml = underlineHtml;
const strikethroughHtml = (text) => `<s>${text}</s>`;
exports.strikethroughHtml = strikethroughHtml;
const codeHtml = (text) => `<code>${text}</code>`;
exports.codeHtml = codeHtml;
const preHtml = (text, language = '') => {
    return language
        ? `<pre><code class="language-${language}">${text}</code></pre>`
        : `<pre>${text}</pre>`;
};
exports.preHtml = preHtml;
const linkHtml = (text, url) => {
    return `<a href="${(0, exports.escapeHtml)(url)}">${text}</a>`;
};
exports.linkHtml = linkHtml;
/** Экранирует пользовательский текст перед вставкой в HTML. */
const escapeHtml = (text) => {
    // Амперсанд заменяем первым, чтобы не изменить добавленные ниже HTML-сущности.
    return text
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
};
exports.escapeHtml = escapeHtml;
