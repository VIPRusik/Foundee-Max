import type { Context } from '../core/context';
import type { MiddlewareFn } from '../core/middleware';
import type { SessionOptions, SessionProperty } from './types';
/** Добавляет в Context состояние пользователя и чата между событиями. */
export declare function session<S extends object & NonNullable<C[P]>, C extends Context & {
    [key in P]?: C[P];
}, P extends SessionProperty<C> = 'session'>(options?: SessionOptions<S, C, P>): MiddlewareFn<C>;
