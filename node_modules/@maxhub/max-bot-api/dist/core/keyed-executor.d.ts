/** Выполняет задачи с одинаковым ключом по очереди, не блокируя остальные ключи. */
export declare class KeyedExecutor {
    private readonly tails;
    run<T>(key: string, task: () => Promise<T>): Promise<T>;
}
