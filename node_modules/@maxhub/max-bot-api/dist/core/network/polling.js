"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Polling = void 0;
const promises_1 = require("node:timers/promises");
const debug_1 = __importDefault(require("debug"));
const api_1 = require("./api");
const debug = (0, debug_1.default)('max:polling');
const BASE_DELAY_MS = 5000; // ms
const MAX_DELAY_MS = 60000; // ms
class Polling {
    constructor(api, abortController, allowedUpdates = []) {
        this.api = api;
        this.abortController = abortController;
        this.allowedUpdates = allowedUpdates;
        this.loop = async (handleUpdate) => {
            debug('Starting long polling');
            let delayOnError = BASE_DELAY_MS;
            while (!this.abortController.signal.aborted) {
                try {
                    const { updates, marker } = await this.api.getUpdates(this.allowedUpdates, {
                        marker: this.marker,
                        signal: this.abortController.signal,
                    });
                    delayOnError = BASE_DELAY_MS;
                    this.marker = marker;
                    await Promise.all(updates.map(handleUpdate));
                }
                catch (err) {
                    if (err instanceof Error) {
                        if (err.name === 'AbortError') {
                            debug('Long polling aborted');
                            return;
                        }
                        if (this.shouldRetry(err)) {
                            debug(`Failed to fetch updates, retrying after ${delayOnError}ms.`, err);
                            try {
                                await (0, promises_1.setTimeout)(delayOnError, undefined, {
                                    signal: this.abortController.signal,
                                });
                            }
                            catch (timeoutErr) {
                                if (timeoutErr instanceof Error && timeoutErr.name === 'AbortError') {
                                    debug('Long polling retry timeout aborted');
                                    return;
                                }
                                throw timeoutErr;
                            }
                            delayOnError = Math.min(delayOnError * 2, MAX_DELAY_MS);
                            continue;
                        }
                    }
                    throw err;
                }
            }
        };
        this.stop = () => {
            debug('Stopping long polling');
            this.abortController.abort();
        };
        this.shouldRetry = (err) => {
            if (!(err instanceof Error))
                return false;
            if (err instanceof api_1.MaxError)
                return err.status === 429 || err.status >= 500;
            return err.name === 'TypeError';
        };
    }
}
exports.Polling = Polling;
