import FormDataStream from 'form-data';
import { StreamUploadOptions } from './types';
export declare class StreamUploadClient {
    post<T>(url: string, body: FormDataStream | string | Buffer, options?: StreamUploadOptions, token?: string): Promise<{
        data: T;
    }>;
}
