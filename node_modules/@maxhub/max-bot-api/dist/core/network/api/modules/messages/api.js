"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MessagesApi = void 0;
const promises_1 = require("node:timers/promises");
const debug_1 = __importDefault(require("debug"));
const base_api_1 = require("../../base-api");
const error_1 = require("../../error");
const const_1 = require("./const");
const debug = (0, debug_1.default)('max:messages');
class MessagesApi extends base_api_1.BaseApi {
    constructor() {
        super(...arguments);
        this.get = async ({ ...query }) => {
            return this._get('messages', {
                query,
            });
        };
        this.getById = async ({ message_id }) => {
            return this._get('messages/{message_id}', {
                path: { message_id },
            });
        };
        this.send = async ({ chat_id, user_id, disable_link_preview, ...body }, options) => {
            const signal = options?.signal;
            let lastError;
            for (let attempt = 0; attempt < const_1.SEND_MESSAGE_RETRIES_COUNT; attempt += 1) {
                signal?.throwIfAborted();
                try {
                    return await this._post('messages', {
                        body,
                        query: { chat_id, user_id, disable_link_preview },
                        signal,
                    });
                }
                catch (err) {
                    const isCriticalError = !(err instanceof error_1.MaxError) || err.code !== 'attachment.not.ready';
                    if (isCriticalError) {
                        throw err;
                    }
                    lastError = err;
                    const delay = const_1.SEND_MESSAGE_RETRY_DELAY_BASE_TIME * (2 ** attempt);
                    debug('Attachment not ready (attempt %d/%d), retrying in %dms', attempt + 1, const_1.SEND_MESSAGE_RETRIES_COUNT, delay);
                    await (0, promises_1.setTimeout)(delay, undefined, { signal });
                }
            }
            throw lastError ?? new error_1.MaxError(500, {
                code: 'attachment.not.ready',
                message: `Attachment not ready after ${const_1.SEND_MESSAGE_RETRIES_COUNT} retries`,
            });
        };
        this.edit = async ({ message_id, ...body }) => {
            return this._put('messages', {
                query: { message_id },
                body,
            });
        };
        this.delete = async ({ ...query }) => {
            return this._delete('messages', {
                query,
            });
        };
        this.getVideoInfo = async ({ ...query }) => {
            return this._get('videos/{video_token}', {
                query,
            });
        };
        this.answerOnCallback = async ({ callback_id, ...body }) => {
            return this._post('answers', {
                query: { callback_id },
                body,
            });
        };
    }
}
exports.MessagesApi = MessagesApi;
