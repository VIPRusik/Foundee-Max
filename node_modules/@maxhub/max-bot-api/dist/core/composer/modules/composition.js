"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.drop = exports.optional = exports.branch = exports.catchMiddleware = exports.lazy = exports.tap = exports.fork = exports.compose = exports.passThrough = exports.pass = exports.concat = exports.flatten = void 0;
const noop = () => Promise.resolve();
const flatten = (middleware) => {
    // Для объекта вызываем middleware() при каждом событии, чтобы учитывать
    // изменения, сделанные после сборки цепочки.
    return typeof middleware === 'function'
        ? middleware
        : (ctx, next) => middleware.middleware()(ctx, next);
};
exports.flatten = flatten;
const concat = (first, andThen) => {
    return async (ctx, next) => {
        let nextCalled = false;
        await first(ctx, async () => {
            // Повторный `next()` ещё раз запустил бы следующие обработчики.
            if (nextCalled)
                throw new Error('`next` already called before!');
            nextCalled = true;
            await andThen(ctx, next);
        });
    };
};
exports.concat = concat;
const pass = (_ctx, next) => next();
exports.pass = pass;
const passThrough = () => exports.pass;
exports.passThrough = passThrough;
const compose = (middlewares) => {
    if (!Array.isArray(middlewares))
        throw new TypeError('Middlewares must be an array');
    if (middlewares.length === 0)
        return exports.pass;
    return middlewares.map(exports.flatten).reduce(exports.concat);
};
exports.compose = compose;
/** Одновременно запускает переданный обработчик и оставшуюся цепочку. */
const fork = (middleware) => {
    const handler = (0, exports.flatten)(middleware);
    return async (ctx, next) => {
        // Если одна из веток завершится с ошибкой, fork также завершится с ошибкой.
        await Promise.all([handler(ctx, noop), next()]);
    };
};
exports.fork = fork;
/** Выполняет дополнительное действие перед переходом к `next()`. */
const tap = (middleware) => {
    const handler = (0, exports.flatten)(middleware);
    return async (ctx, next) => {
        // Переданный обработчик получает `next()` без продолжения и не запускает основную цепочку.
        await handler(ctx, noop);
        await next();
    };
};
exports.tap = tap;
/** Вызывает `factory` для каждого события и выполняет возвращённый обработчик. */
const lazy = (factory) => {
    if (typeof factory !== 'function')
        throw new TypeError('Factory must be a function');
    return async (ctx, next) => {
        // Выбор может зависеть от сессии, прав пользователя и других данных Context.
        const middleware = await factory(ctx);
        return (0, exports.flatten)(middleware)(ctx, next);
    };
};
exports.lazy = lazy;
/** Передаёт ошибку из указанной цепочки в `errorHandler`. */
const catchMiddleware = (errorHandler, ...middlewares) => {
    const handler = (0, exports.compose)(middlewares);
    return async (ctx, next) => {
        try {
            await handler(ctx, next);
        }
        catch (error) {
            // Если errorHandler выбросит новую ошибку, она будет передана вызывающему коду.
            await errorHandler(error, ctx);
        }
    };
};
exports.catchMiddleware = catchMiddleware;
/** Выполняет одну из двух веток по синхронному или асинхронному условию. */
const branch = (predicate, onTrue, onFalse) => {
    if (typeof predicate === 'boolean') {
        return (0, exports.flatten)(predicate ? onTrue : onFalse);
    }
    return (0, exports.lazy)(async (ctx) => {
        return await predicate(ctx) ? onTrue : onFalse;
    });
};
exports.branch = branch;
/** Выполняет переданную цепочку при true и сразу вызывает `next()` при false. */
const optional = (predicate, ...middlewares) => (0, exports.branch)(predicate, (0, exports.compose)(middlewares), (0, exports.passThrough)());
exports.optional = optional;
/** Прекращает обработку события при true и вызывает `next()` при false. */
const drop = (predicate) => {
    return (0, exports.branch)(predicate, noop, (0, exports.passThrough)());
};
exports.drop = drop;
